---
title: "Linux入门：安装系统"
published: 2025-08-25 17:21:31
category: 笔记
tags: [Linux, 教程]
---

# Linux系统的安装

## 1. 系统的选择

Linux发展了几十年，到今天已经诞生了上百种发行版
![](https://www.zhx-blog.top/usr/uploads/2025/08/2567349419.jpg)
我个人建议新手使用Ubnutu系的发行版（或debian系），软件源十分丰富，而且入手难度低；

> 当然，国产系统也是不错的选择，比如Deppin、统信USO

这里以Ubuntu系的LinuxMint为例来演示，这也是我现在正在使用的发行版。

## 下载ISO镜像文件

访问[LinuxMint官网](https://www.linuxmint.com/)（或其他发行版官网）[下载ISO系统镜像](https://mirrors.aliyun.com/linuxmint/stable/22.1/linuxmint-22.1-cinnamon-64bit.iso)，里面有很多镜像地址，如alibaba的。

## 制作启动盘

目前启动盘制作主要可以用以下几种方法（都要准备一个至少8G的U盘）：

### 方法1：将文件写入U盘中

优点：~~历史悠久~~，稳定；
缺点：每次制作都要格式化U盘，且只能同时制作1张

### 方法2：使用Ventoy

Ventoy只需要在U盘中放入镜像文件（放进去就行）即可作为启动盘，可以一次放入多个系统镜像，也可以放入杂七杂八的文件。~~建议使用64G以上的U盘（最低8G不影响）因为以后你可能像我一样放七八个镜像文件、一包安装包和一堆备份文件~~

> 注意：请确保U盘容量正常不虚标，否则制作将失败

首先前往[官网](https://www.ventoy.net/cn/)下载相应系统最新版本的**Ventoy**，插入U盘，正对电脑启动方式选择对应的分区类型。

> 简单来说，**你无法在MBR磁盘上安装UEFI模式的操作系统，也无法在GPT磁盘上安装传统的BIOS模式操作系统。**

所以如果待重装系统的电脑**是UEFI模式**，则需要选择**GPT类型**，反之则选择**MBR类型**。

一般来讲，新电脑一般是UEFI模式的。

插入U盘后

![](https://www.zhx-blog.top/usr/uploads/2025/08/1656605217.png)

在`设备`中选择到你的U盘，点击安装（确认U盘内没有重要文件），就会**格式化U盘**并安装Ventoy到你的U盘上。此时会创建两个分区，

![](https://www.zhx-blog.top/usr/uploads/2025/08/2781680088.png)

其中Ventoy用于放置你的ISO文件或其他需要存储的文件，VTOYEFI则是引导电脑寻找文件的分区。安装好并放入刚才下载的ISO文件后，重启电脑至**启动设备菜单**（需要按下特殊按键。

> **常见的按键有哪些？（需要快速连按）**
>
> - **最常见的是 `F12` 和 `Esc`**
> - **华硕 (ASUS)**：`F8`
> - **技嘉 (GIGABYTE)**：`F12`
> - **微星 (MSI)**：`F11`
> - **联想 (Lenovo)**：`F12` 或 笔记本侧的 **Novo键**
> - **戴尔 (Dell)**：`F12`
> - **惠普 (HP)**：`F9` 或 `Esc`
> - **宏碁 (Acer)**：`F12`
> - **三星 (Samsung)**：`Esc` 或 `F2`、`F10`
> - **Surface**：按住 **音量减键** 再按一下电源键

随后选择通过你的U盘启动。随后你会打开一个体验LinuxMint的界面，双击运行桌面上的引导程序，你就可以根据提示与选项安装系统了。

> 注意：
>
> 安装后可能出现无法联网的现象，这是因为驱动缺失，建议使用网线/蓝牙先暂时连接网络，等按照**欢迎画面**的提示把
>
> ![](https://www.zhx-blog.top/usr/uploads/2025/08/235085004.png)
>
> 驱动和软件更新安装好就行了

## 常用软件推荐：

首先可以安装**QQ**和**微信**，使用Firefox搜索进入官网下载即可。建议下载AppImage包，这个给到运行后即可直接使用，卸载只需将该文件删除；

```bash
# 进入下载目录
cd Downloads/
# 授予执行权限
chmod +x QQ_*.AppImage # 换成实际文件名
# 或者使用通配符
chmod +x *.AppImage
```

或下载.deb安装包

```bash
# 建议使用apt安装，避免依赖问题
sudo apt install ./QQ_*.deb # 一定要加上`./`，告诉apt于本地寻找文件而非在线查找
# 或者使用dpkg，最原始的方式
sudo dpkg -i QQ_*.deb # `./`加不加皆可
```

> 其他系统自行搜索教程

### 其他生产力工具：

**markdown编辑器**：Typora

```bash
sudo apt install typora
```

**代码编辑器**：sublime text

```bash
sudo apt install sublime-text
```

**修图软件**：GIMP（Linux上的PS）

```bash
sudo apt install gimp
```

### 游戏推荐

**经典中的经典：**steam

```bash
sudo apt install steam
```

**吉祥物Tux**：

```bash
sudo apt install supertux
sudo apt install supertuxkart
sudo apt install extremetuxracer
```

**开元RTS：**Warzone2100

```bash
sudo apt install warzone2100
```

剩下的你们就自行探索啦！
